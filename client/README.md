# Client

Frontend application for the inventory system.

Application stack:
- React
- Vite
- TypeScript

### Requirement
- Node Packet Manager
- NodeJS
- Docker

### Installation

To run locally, Run the following CLI command:
```
npm install
npm run dev
````

You can also setup the project by running the compose at the root folder.
```
docker compose up -d --build
```
